name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@v0.1.7
      with:
        host: 138.68.130.22
        key: ${{ secrets.MY_KEYS }}
        
        script: cd cd /home/winc/My-Proxy
                git pull